# Modal AI stages

All AI model download and inference lives here (SAM 3.1, ball, pose, actions, …).

**Do not** install model weights on the laptop or in `worker/`.

See [docs/AI_POLICY.md](../docs/AI_POLICY.md).

Local v0 next step: add `track_players` SAM 3.1 function invoked from the Cloud Run Job / local worker.
