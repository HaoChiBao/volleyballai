"""
Modal ball tracking stubs.

Canonical deploy: `modal deploy modal_app/app.py` (track_ball + track_ball_fast).
"""

from __future__ import annotations

print(
    "Use modal_app/app.py — deploys track_ball (HQ sliding-window) "
    "and track_ball_fast (non-overlapping).",
)
